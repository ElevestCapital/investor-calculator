<div class="price-bar-wrapper">
  <div class="price-bar-group">
    
    <template x-for="(year, i) in selectedYears">
      <div class="price-bar-wrapper">
        <div class="price-bar" :style="{'--value': year.cashFlow, '--maxValue': 1000000}">
          <div class="popup" x-text="toSpecialNotation(year.cashFlow)" >POPUP</div>
        </div>
        <div>Year</div>
        <div x-text="year.number"></div>
      </div>
    </template>
    
  </div>
</div>

<style>
  .year-number {
    width: 100%;
    text-align: center;
  }

  .popup {
    position: absolute;
    top: -2em;
    display: none;
  }

  .price-bar-wrapper {
    position: relative;
    grid-column-start: 2;
    grid-column-end: 4;
  }
  
  .price {
    display: flex;
    justify-content: space-between;
  }
  
  @keyframes grow-bar {
    from: {
      height: 0%
    } to {
      height: 550px;
    }
  }
  
  .price-bar-group {
    display: flex;
    gap: 1rem;
    align-items: end;
    height: 0px;
    justify-content: end;
    position: absolute;
    right: 0;
    bottom: 0;
    pointer-events: none;
    animation: grow-bar 1s ease-in-out forwards;
    background: url('./lines.svg');
    width: 100%;
    
    @media (max-width: 1200px) {
      position: static;
      max-height: 10rem;
    }
  }
  
  .price-bar-wrapper {
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: end;
    position: relative;
    width: 100%;
  }
  
  .price-bar:hover .popup {
    display: block;
  }
  
  .price-bar:hover {
    background-color: var(--color-orange-default);
  }
  
  .price-bar {
    --value: 1000;
    --maxValue: 2000;
    --percent: calc(var(--value) / var(--maxValue));
    height: calc(100% * var(--percent));
    width: 100%;
    background-color: var(--color-orange-dark);
    display: flex;
    align-items: end;
    justify-content: center;
    padding: 0 0.5rem;
    position: relative;
    pointer-events: all;
  }
</style>