<div class="price-bar-group">
  <img class="lines" src="./lines.svg" alt="">
    <template x-for="(year, i) in selectedYears">
      <div class="price-bar-wrapper">
        <div class="price-bar" :style="{'--value': year.totalInvested, '--maxValue': getBiggerValue()}">
          <div class="popup" x-text="toSpecialNotation(year.totalInvested)" >POPUP</div>
        </div>
        <div x-text="'Year ' + year.number"></div>
      </div>
    </template>
</div>

<style>
  .lines {
    position: absolute;
    width: 79%;
  }

  .popup {
    position: absolute;
    top: -2em;
    display: none;
  }

  .pricing-calculator-wrapper {
  display: flex;
  gap: 2rem;
  align-items: start;
}

.price {
  display: flex;
  justify-content: space-between;
}

.price-bar-group {
  display: flex;
  gap: 0.5rem;
  align-items: end;
  height: 400px;
  justify-content: end;
  position: absolute;
  right: 0;
  bottom: 0;
  padding: 5rem;
  pointer-events: none;
}

.price-bar-wrapper {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: end;
  position: relative;
}

.price-bar:hover .popup {
  display: block;
}

.price-bar:hover {
  background-color: var(--orange-default);
}

.price-bar {
  --value: 1000;
  --maxValue: 2000;
  --percent: calc(var(--value) / var(--maxValue));
  height: calc(100% * var(--percent));
  width: 40px;
  background-color: var(--orange-dark);
  display: flex;
  align-items: end;
  justify-content: center;
  padding: 0 0.5rem;
  position: relative;
  pointer-events: all;
}
</style>