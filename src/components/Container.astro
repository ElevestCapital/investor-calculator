<div class="container" {...Astro.props}>
  <slot />
</div>

<style lang="scss">
  .container {
    width: 100%;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
    width: 100%;
    margin: auto;
    position: relative;
    gap: 2rem;
    @media (max-width: 767px) {
      padding: 1rem;
    }
  }

  /* Embed-specific container styles */
  html.is-embedded .container {
    min-height: 100%;
    height: 100vh;
    overflow: hidden;
    padding: 1rem;
  }

  html.is-embedded .container .layout {
    height: 100%;
    overflow-y: auto;
    overflow-x: hidden;
  }
</style>
